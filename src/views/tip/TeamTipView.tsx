import React, {FC} from "react";
import {WaiterInfoNarrow} from "src/models/application/waiter"

interface Props {
    waiterInfo: WaiterInfoNarrow
}

const TeamTipView: FC<Props> = (props) => {
    return (
        <div>

        </div>
    );
};

export default TeamTipView;
