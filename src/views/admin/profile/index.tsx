import React, {FunctionComponent} from "react";

interface Props {
}

const AdminProfileView: FunctionComponent<Props> = (props) => {
    return (
        <h1>profile</h1>
    );
};

export default AdminProfileView;
